# MagicSniffer 功能规划

> 本文档记录未来计划添加的功能，按优先级排序。实现时每完成一项请勾选并标注版本号。

## 状态图例

- [ ] 待规划
- [x] 已完成（标注版本）

---

## 一、高价值（磁盘工具核心诉求）

### 1. 扫描快照对比（占用变化追踪） ✅

**状态**：[x] 已完成（1.1.0）

**目标**：让用户看到"空间怎么又满了"的答案。

- 保存每次扫描结果快照（复用现有 `cache.rs` 持久化能力）
- 两次扫描对比，标记目录：
  - 🟢 增大（绝对值 + 增长率）
  - 🔴 减小
  - ⚪ 无变化
- 树图/列表两种视图展示差异
- 关联文件分类，定位是缓存膨胀还是下载堆积

**架构切入点**：新增 `snapshots.rs` 快照子系统（每次完整扫描自动保存，同路径最多 10 份 FIFO 淘汰）+ `diff_snapshots` 命令计算差异树；前端新增 DiffView 独立页面（快照选择 / 汇总卡 / 分类条 / 树图差异染色 / 列表视图）。

### 2. 重复文件检测

**目标**：找出占空间的大体积重复文件。

- 按文件大小分组（同大小才可能是重复）
- 小文件直接 hash，大文件先采样 hash 再全量确认
- 结果按"可释放空间"排序展示
- 与删除风险评估天然衔接：重复文件 = 低风险可删
- 支持从重复组中保留一份，其余移入废纸篓

**架构切入点**：新建 `src-tauri/src/duplicates.rs`，复用 `categorizer` + `risk` 评估。

### 3. 大文件 + 时间维度排序

**目标**：快速定位空间消耗点。

- 一键列出 Top 100 大文件（当前仅有目录树图，缺文件级列表）
- 按扩展名聚合统计（.mp4 总共占多少、.log 有多少）
- 树图按"最后修改时间"着色（暖色=近期，冷色=久远），定位陈年垃圾
- 时间范围筛选（如"只显示 1 年未访问的文件"）

**架构切入点**：`FileNode` 已有 `modified_at` 字段，扫描结果可直接复用。

### 4. 一键清理（带废纸篓保护）

**目标**：把安全项聚合为清理清单，一键操作。

- 聚合分类为"安全"的项目：缓存、日志、临时文件、垃圾桶、语言包
- 清理清单预览（选中项 + 可释放空间合计）
- 一键移入废纸篓（非永久删除，可恢复）
- 前端记录清理历史，提供一键撤销
- 危险项（系统文件、应用）默认禁用并提示

**架构切入点**：复用 `risk` 评估 + `reveal_in_file_manager` 的路径操作，新增 `cleanup.rs`。

---

## 二、中价值（健康度扩展，已有 smartctl 基础）

### 5. SMART 历史趋势 + 告警

**目标**：从"当前状态"升级为"趋势监控"。

- 周期性（如每小时）记录 SMART 数据：温度、已用寿命、剩余备用块、错误数
- 绘制时间趋势图（温度曲线、寿命消耗曲线）
- 异常告警：
  - 温度超过阈值 → 托盘弹通知
  - 已用寿命接近 100% → 红色告警
  - 错误数持续增长 → 建议备份
- `notify` crate 已在依赖中，可做目录变化检测补充告警依据

**架构切入点**：新建 `src-tauri/src/health_history.rs`，复用 `parse_smartctl()`。

### 6. 磁盘测速

**目标**：展示磁盘 IO 性能。

- 顺序读 / 顺序写基准测试
- 展示吞吐量（MB/s）、IOPS
- 与型号、接口协议（Apple Fabric / SATA / USB）关联展示
- 测试期间避免阻塞 UI（后台线程 + 进度事件）

---

## 三、交互提升

### 7. 拖拽扫描 + 命令面板

**目标**：降低使用门槛。

- 文件夹拖入窗口直接启动扫描
- ⌘K 风格命令面板：搜索、扫描、清理、设置快捷键直达
- 全局快捷键（托盘驻留时）唤起应用

### 8. 导出报告

**目标**：分享与归档。

- 扫描结果导出 JSON / CSV
- 生成可视化报告（HTML 单文件，含树图缩略 + 分类统计）
- 支持命令行参数直接导出（无头模式）

### 9. 深色模式

**目标**：跟随系统外观。

- 当前 UI 为固定浅色系
- 适配 macOS 深浅色自动切换（`prefers-color-scheme`）
- Tailwind CSS 4 已有 dark: 变体支持，改造成本低

---

## 四、增强现有能力

### 10. 文件类型透视

**目标**：多维度审视空间。

- 树图支持切换维度：目录树 / 扩展名聚合
- "按类型查看"视图：各大扩展名占用面积
- 目录内下钻后保持当前类型维度

### 11. 清理排除名单

**目标**：用户掌控清理边界。

- 记录用户不想清理的路径
- 风险评估与一键清理时自动跳过排除项
- 设置面板管理排除名单（增删改）

---

## 版本规划建议

| 版本 | 建议内容 |
|------|----------|
| 1.1.0 | #1 扫描快照对比 |
| 1.2.0 | #2 重复文件检测 |
| 1.3.0 | #3 大文件排序 + #4 一键清理 |
| 2.0.0 | #5 SMART 趋势 + #7 拖拽扫描 + #9 深色模式 |
| 后续 | #6 测速、#8 导出、#10 类型透视、#11 排除名单 |
